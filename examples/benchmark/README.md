# Dataset Benchmark Example

This directory contains configuration files for `examples/benchmark_dataset.py`.

- Common config: `config/common.yaml`
- Method configs: `config/methods/*.yaml`

Run a dry-run plan:

```bash
uv run python examples/benchmark_dataset.py --dry-run --sample-limit 2
```
